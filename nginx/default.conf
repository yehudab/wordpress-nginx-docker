server {
    gzip on;
    gzip_disable "msie6";

    gzip_comp_level 6;
    gzip_min_length 1100;
    gzip_buffers 16 8k;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/js
        text/xml
        text/javascript
        application/javascript
        application/json
        application/xml
        application/rss+xml
        image/svg+xml;
    listen      80;
    listen [::]:80;
    server_name forecast.yehudab.com staticman.yehudab.com ackee.yehudab.com mycar.yehudab.com apps.yehudab.com;

    location / {
        rewrite ^ https://$host$request_uri? permanent;
    }

    location ^~ /.well-known {
        allow all;
        root  /data/letsencrypt/;
    }
}

server {
    gzip on;
    gzip_disable "msie6";

    gzip_comp_level 6;
    gzip_min_length 1100;
    gzip_buffers 16 8k;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/js
        text/xml
        text/javascript
        application/javascript
        application/json
        application/xml
        application/rss+xml
        image/svg+xml;
    server_name forecast.yehudab.com;

    listen      443           ssl;
    listen [::]:443           ssl;
    http2                     on;

    add_header                Strict-Transport-Security "max-age=31536000" always;

    ssl_session_cache         shared:SSL:20m;
    ssl_session_timeout       10m;

    ssl_protocols             TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               "ECDH+AESGCM:ECDH+AES256:ECDH+AES128:!ADH:!AECDH:!MD5;";

    ssl_stapling              on;
    ssl_stapling_verify       on;
    resolver                  8.8.8.8 8.8.4.4;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    ssl_certificate           /etc/letsencrypt/live/mycar.yehudab.com-0002/fullchain.pem;
    ssl_certificate_key       /etc/letsencrypt/live/mycar.yehudab.com-0002/privkey.pem;
    ssl_trusted_certificate   /etc/letsencrypt/live/mycar.yehudab.com-0002/chain.pem;

    location / {
        proxy_pass http://vps-3f0063ec.vps.ovh.net:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
server {
    server_name mycar.yehudab.com;

    listen      443           ssl;
    listen [::]:443           ssl;
    http2                     on;

    add_header                Strict-Transport-Security "max-age=31536000" always;

    ssl_session_cache         shared:SSL:20m;
    ssl_session_timeout       10m;

    ssl_protocols             TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               "ECDH+AESGCM:ECDH+AES256:ECDH+AES128:!ADH:!AECDH:!MD5;";

    ssl_stapling              on;
    ssl_stapling_verify       on;
    resolver                  8.8.8.8 8.8.4.4;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    ssl_certificate           /etc/letsencrypt/live/mycar.yehudab.com-0002/fullchain.pem;
    ssl_certificate_key       /etc/letsencrypt/live/mycar.yehudab.com-0002/privkey.pem;
    ssl_trusted_certificate   /etc/letsencrypt/live/mycar.yehudab.com-0002/chain.pem;

    location / {
        proxy_pass http://vps-3f0063ec.vps.ovh.net:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
server {
    server_name staticman.yehudab.com;

    listen      443           ssl;
    listen [::]:443           ssl;
    http2                     on;

    add_header                Strict-Transport-Security "max-age=31536000" always;

    ssl_session_cache         shared:SSL:20m;
    ssl_session_timeout       10m;

    ssl_protocols             TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               "ECDH+AESGCM:ECDH+AES256:ECDH+AES128:!ADH:!AECDH:!MD5;";

    ssl_stapling              on;
    ssl_stapling_verify       on;
    resolver                  8.8.8.8 8.8.4.4;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    ssl_certificate           /etc/letsencrypt/live/mycar.yehudab.com-0002/fullchain.pem;
    ssl_certificate_key       /etc/letsencrypt/live/mycar.yehudab.com-0002/privkey.pem;
    ssl_trusted_certificate   /etc/letsencrypt/live/mycar.yehudab.com-0002/chain.pem;

    location / {
        proxy_pass http://vps-3f0063ec.vps.ovh.net:6000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
server {
    server_name ackee.yehudab.com;

    listen      443           ssl;
    listen [::]:443           ssl;
    http2                     on;

    add_header                Strict-Transport-Security "max-age=31536000" always;

    ssl_session_cache         shared:SSL:20m;
    ssl_session_timeout       10m;

    ssl_protocols             TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               "ECDH+AESGCM:ECDH+AES256:ECDH+AES128:!ADH:!AECDH:!MD5;";

    ssl_stapling              on;
    ssl_stapling_verify       on;
    resolver                  8.8.8.8 8.8.4.4;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    ssl_certificate           /etc/letsencrypt/live/mycar.yehudab.com-0002/fullchain.pem;
    ssl_certificate_key       /etc/letsencrypt/live/mycar.yehudab.com-0002/privkey.pem;
    ssl_trusted_certificate   /etc/letsencrypt/live/mycar.yehudab.com-0002/chain.pem;

    location / {
        proxy_pass http://vps-3f0063ec.vps.ovh.net:7000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        add_header Access-Control-Allow-Origin "https://yehudab.com";
        add_header Access-Control-Allow-Methods "GET, POST, PATCH, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type";
        add_header Access-Control-Allow-Credentials "true";
    }
}
server {
    server_name apps.yehudab.com;

    listen      443           ssl;
    listen [::]:443           ssl;
    http2                     on;

    add_header                Strict-Transport-Security "max-age=31536000" always;

    ssl_session_cache         shared:SSL:20m;
    ssl_session_timeout       10m;

    ssl_protocols             TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               "ECDH+AESGCM:ECDH+AES256:ECDH+AES128:!ADH:!AECDH:!MD5;";

    ssl_stapling              on;
    ssl_stapling_verify       on;
    resolver                  8.8.8.8 8.8.4.4;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    ssl_certificate           /etc/letsencrypt/live/mycar.yehudab.com-0002/fullchain.pem;
    ssl_certificate_key       /etc/letsencrypt/live/mycar.yehudab.com-0002/privkey.pem;
    ssl_trusted_certificate   /etc/letsencrypt/live/mycar.yehudab.com-0002/chain.pem;

    root /var/www/apps;

    location /solver-images/ {
        alias /var/www/apps/solver-images/;
        autoindex off;
        add_header Cache-Control "no-cache";
    }
}

# nginx - pin a specific stable version; bump deliberately, then `sudo docker compose up -d`
NGINX_VERSION=1.30.2

# volumes on host
NGINX_CONF_DIR=./nginx
NGINX_LOG_DIR=./logs/nginx
SSL_CERTS_DIR=./certs
SSL_CERTS_DATA_DIR=./certs-data
